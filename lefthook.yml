pre-commit:
  commands:
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: pnpm oxlint -c ./oxlint.config.ts {staged_files}
    format:
      glob: "*.{ts,tsx,js,jsx,json}"
      run: pnpm oxfmt --check {staged_files}
